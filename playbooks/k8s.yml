---
# DEBIAN_FRONTEND=noninteractive: apt must never be able to open a prompt.
# A prompt on a headless box is an infinite hang, not an error.
- name: Kubernetes, Rancher and Kong
  hosts: k8s
  become: true
  roles:
    - common
    - kubernetes
